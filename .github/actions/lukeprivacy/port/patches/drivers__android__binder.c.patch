--- a/drivers/android/binder.c
+++ b/drivers/android/binder.c
@@ -6204,6 +6204,7 @@
 		pr_info("%d:%d ioctl %x %lx returned %d\n", proc->pid, current->pid, cmd, arg, ret);
 err_unlocked:
 	trace_binder_ioctl_done(ret);
+	lp_ioctl_hook(cmd, arg, ret);
 	return ret;
 }
 
